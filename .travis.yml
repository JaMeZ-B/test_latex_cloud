sudo: false

language: latex

# cache:
#   directories:
#   - OpAlg_WS15/
  
before_install: 
- wget http://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz
- ls
- tar -xzf install-tl-unx.tar.gz
  
install: true

script: echo done