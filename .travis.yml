sudo: false

language: perl

# cache:
#   directories:
#   - OpAlg_WS15/
  
before_install: 
- wget http://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz
- tar -xzf install-tl-unx.tar.gz
- ls
- cd install-tl-2*
- ls -a
- chmod +x install-tl
- ./install-tl
  
install: true

script: echo done